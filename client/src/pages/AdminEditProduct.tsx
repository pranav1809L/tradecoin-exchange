import { useEffect, useState } from "react";
import { trpc } from "@/lib/trpc";
import { toast } from "sonner";
import { PencilLine, Trash2 } from "lucide-react";
import { AdminFrame, AdminGate, PinConfirm } from "./AdminShared";

function EditForm() {
  const products = trpc.admin.products.useQuery(); const utils = trpc.useUtils();
  const [selectedId, setSelectedId] = useState<number | null>(null); const [name, setName] = useState(""); const [category, setCategory] = useState(""); const [description, setDescription] = useState(""); const [price, setPrice] = useState(""); const [action, setAction] = useState<"save" | "drop" | null>(null);
  const update = trpc.admin.updateProduct.useMutation(); const remove = trpc.admin.deleteProduct.useMutation();
  const selected = products.data?.find((product) => product.id === selectedId);
  useEffect(() => { if (!selected) return; setName(selected.name); setCategory(selected.category); setDescription(selected.description); setPrice(selected.currentPrice); }, [selected]);
  async function save(pin: string) { if (!selectedId) return; try { await update.mutateAsync({ productId: selectedId, name, category, description, price, pin }); setAction(null); await Promise.all([utils.admin.products.invalidate(), utils.markets.products.invalidate()]); toast.success("Product changes saved"); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not save product"); } }
  async function drop(pin: string) { if (!selectedId) return; try { await remove.mutateAsync({ productId: selectedId, pin }); setSelectedId(null); setAction(null); await Promise.all([utils.admin.products.invalidate(), utils.markets.products.invalidate()]); toast.success("Product dropped"); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not drop product"); } }
  function discard() { if (selected) { setName(selected.name); setCategory(selected.category); setDescription(selected.description); setPrice(selected.currentPrice); } setAction(null); }
  return <AdminFrame title="Edit product" description="Select a product to rename, update its description or price, or drop it from the catalog."><div className="grid gap-5 lg:grid-cols-[280px_1fr]"><section className="rounded-2xl border border-white/[0.08] bg-[#0b1823] p-3"><div className="mb-3 px-2 text-xs font-bold uppercase tracking-wider text-slate-500">Existing products</div><div className="space-y-1">{products.data?.map((product) => <button key={product.id} onClick={() => setSelectedId(product.id)} className={`w-full rounded-xl px-3 py-3 text-left transition ${selectedId === product.id ? "bg-[#69ddbd]/10 text-[#8ceacd]" : "text-slate-400 hover:bg-white/[0.04] hover:text-white"}`}><div className="truncate text-sm font-semibold">{product.name}</div><div className="mt-1 text-[10px] text-slate-600">{Number(product.currentPrice).toFixed(2)} TC</div></button>)}</div></section>{selected ? <section className="rounded-2xl border border-white/[0.08] bg-[#0b1823] p-5"><div className="mb-5 flex items-center gap-2"><PencilLine size={18} className="text-violet-300" /><h2 className="text-sm font-bold">Edit details</h2></div><div className="grid gap-4 sm:grid-cols-2"><div><label className="text-xs font-semibold text-slate-300">Product name</label><input value={name} onChange={(event) => setName(event.target.value)} className="field-input mt-2" /></div><div><label className="text-xs font-semibold text-slate-300">Category</label><input value={category} onChange={(event) => setCategory(event.target.value)} className="field-input mt-2" /></div><div><label className="text-xs font-semibold text-slate-300">Current price (TC)</label><input value={price} onChange={(event) => setPrice(event.target.value)} className="field-input mt-2" inputMode="decimal" /></div><div className="sm:col-span-2"><label className="text-xs font-semibold text-slate-300">Description <span className="font-normal text-slate-500">(optional)</span></label><textarea value={description} onChange={(event) => setDescription(event.target.value)} className="field-input mt-2 h-28 py-3" /></div></div><div className="mt-5 flex flex-wrap gap-2"><button onClick={() => setAction("save")} className="rounded-xl bg-[#69ddbd] px-5 py-3 text-xs font-bold text-[#062a2b]">Confirm changes</button><button onClick={() => setAction("drop")} className="flex items-center gap-2 rounded-xl border border-rose-400/25 px-4 py-3 text-xs font-semibold text-rose-300"><Trash2 size={14} /> Drop product</button></div></section> : <section className="grid min-h-64 place-items-center rounded-2xl border border-dashed border-white/10 bg-white/[0.02] text-center text-sm text-slate-500">Select a product to begin editing.</section>}</div><PinConfirm open={action !== null} title={action === "drop" ? "Enter your PIN to permanently drop this product." : "Enter your PIN to save these product changes."} onConfirm={(pin) => void (action === "drop" ? drop(pin) : save(pin))} onCancel={discard} pending={update.isPending || remove.isPending} /></AdminFrame>;
}
export default function AdminEditProduct() { return <AdminGate><EditForm /></AdminGate>; }
