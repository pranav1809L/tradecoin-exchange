import { useTheme } from "@/contexts/ThemeContext";
import { trpc } from "@/lib/trpc";
import { ArrowDownRight, ArrowLeft, ArrowUpRight, Globe2, RefreshCw, UserRound } from "lucide-react";
import { Link } from "wouter";

function money(value: string | number | null | undefined) {
  return `${Number(value ?? 0).toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 })} TC`;
}

export default function AllOnlineTrades() {
  const { theme } = useTheme();
  const tradesQuery = trpc.allTrades.list.useQuery(undefined, { refetchInterval: 5_000 });
  const trades = tradesQuery.data ?? [];
  const isLight = theme === "light";
  return <div className={`site-shell min-h-screen ${isLight ? "bg-[#f4f7f8] text-slate-900" : "bg-[#071019] text-slate-100"}`}><header className={`border-b ${isLight ? "border-slate-200 bg-white/90" : "border-white/[0.07] bg-[#071019]/90"}`}><div className="mx-auto flex h-[76px] max-w-6xl items-center justify-between px-4 sm:px-6"><Link href="/" className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-gradient-to-br from-[#66e0bd] to-[#1ca4a7] text-[#062a2b]"><Globe2 size={19} /></span><span className="text-lg font-bold">trade<span className="text-[#68e0c0]">coin</span></span></Link><Link href="/" className="flex items-center gap-2 rounded-lg border border-white/10 px-3 py-2 text-xs font-semibold text-slate-300 hover:text-white"><ArrowLeft size={14} /> Back to exchange</Link></div></header><main className="mx-auto max-w-6xl px-4 py-8 sm:px-6"><div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><div className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.22em] text-[#68e0c0]"><span className="h-1.5 w-1.5 rounded-full bg-[#68e0c0]" /> Live public feed</div><h1 className="mt-2 text-4xl font-bold tracking-tight">All Online Trades</h1><p className={`mt-2 text-sm ${isLight ? "text-slate-500" : "text-slate-400"}`}>Every completed trade happening across all TradeCoin accounts.</p></div><button onClick={() => void tradesQuery.refetch()} disabled={tradesQuery.isFetching} className="flex items-center justify-center gap-2 rounded-xl border border-[#69ddbd]/30 px-4 py-2.5 text-xs font-bold text-[#69ddbd] transition hover:bg-[#69ddbd]/10 disabled:opacity-60"><RefreshCw size={14} className={tradesQuery.isFetching ? "animate-spin" : ""} /> Refresh</button></div><div className={`mb-5 flex items-center justify-between rounded-xl border p-3 text-xs ${isLight ? "border-slate-200 bg-white text-slate-500" : "border-white/[0.08] bg-white/[0.03] text-slate-400"}`}><span>Auto-refreshing every 5 seconds</span><span className="font-mono text-[#69ddbd]">{trades.length} trades loaded</span></div>{trades.length === 0 ? <div className={`grid min-h-64 place-items-center rounded-2xl border text-center text-sm ${isLight ? "border-slate-200 bg-white text-slate-500" : "border-white/[0.08] bg-[#0b1823] text-slate-500"}`}>No completed trades yet.<br /><span className="mt-1 text-xs">Matched trades will appear here for everyone.</span></div> : <div className="space-y-3">{trades.map(({ trade, product, buyerName, sellerName }) => <article key={trade.id} className={`rounded-2xl border p-4 ${isLight ? "border-slate-200 bg-white shadow-sm" : "border-white/[0.08] bg-[#0b1823]"}`}><div className="flex flex-col gap-4 md:flex-row md:items-center"><div className="flex min-w-0 flex-1 items-center gap-3"><div className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-[#69ddbd]/10 text-[#69ddbd]"><span className="text-xs font-black">{product.name.slice(0, 2).toUpperCase()}</span></div><div className="min-w-0"><h2 className="truncate text-sm font-bold">{product.name.replace("Seed • ", "")}</h2><p className={`mt-1 text-[10px] uppercase tracking-[0.14em] ${isLight ? "text-slate-400" : "text-slate-600"}`}>{new Date(trade.executedAt).toLocaleString()}</p></div></div><div className="flex items-center gap-3 text-xs"><div className="text-right"><div className="mb-1 text-[10px] uppercase tracking-wider text-slate-500">Buyer</div><div className="flex items-center gap-1 font-semibold text-emerald-400"><ArrowDownRight size={13} /> {buyerName}</div></div><div className="text-slate-600">→</div><div><div className="mb-1 text-[10px] uppercase tracking-wider text-slate-500">Seller</div><div className="flex items-center gap-1 font-semibold text-rose-400"><ArrowUpRight size={13} /> {sellerName}</div></div></div><div className="grid grid-cols-3 gap-5 text-right"><div><div className="text-[10px] uppercase tracking-wider text-slate-500">Price</div><div className="mt-1 font-mono text-sm font-bold">{money(trade.price)}</div></div><div><div className="text-[10px] uppercase tracking-wider text-slate-500">Qty</div><div className="mt-1 font-mono text-sm font-bold">{trade.quantity}</div></div><div><div className="text-[10px] uppercase tracking-wider text-slate-500">Total</div><div className="mt-1 font-mono text-sm font-bold text-[#69ddbd]">{money(trade.totalValue)}</div></div></div></div></article>)}</div>}</main></div>;
}
